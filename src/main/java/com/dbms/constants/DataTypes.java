package com.dbms.constants;

public class DataTypes {

    public static final String[] DATATYPE = {"VARCHAR", "TEXT", "INT", "BOOLEAN", "FLOAT", "DOUBLE"};
    public static enum DataType {
        VARCHAR,
        TEXT,
        INT,
        BOOLEAN,
        FLOAT,
        DOUBLE
    }
    private DataTypes() {
    }
}
